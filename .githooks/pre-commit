#!/usr/bin/env bash

# go fmt ./...
# goimports -l -w . # includes go fmt
# golint ./...
# go vet ./...
cd src/backend &&  golangci-lint run --fix --disable-all --enable=gocognit --enable=unused && gocyclo -over 10 .